 1. benchmark_mac -> 성능측정
    1-1. 2차원 배열과 2차원 필터를 인자로 받습니다.
    1-2. repeat = 10 / total_time = 0 / 1초 = 1000밀리초
    1-3. time_perf_counter() (스톱워치)
        => 고해상도의 시간 측정용 숫자를 반환합니다.
        => 고해상도 -> 아주 짧은 작업 시간을 측정한다는 의미입니다.
        => time.perf_counter()는 호출할 떄마다 새로운 시간 측정이 아닙니다.
        => 하나의 시간 카운터가 계속 흐르고 있고 호출할 떄마다 그 지점을 반환합니다.
    1-4. mac(input_data, filter_data)를 통해 계산합니다.
    1-5. time_perf_counter 한 번 더 호출합니다.
    1-6. 그리고 end - start를 통해 계산에 걸린 시간을 알아냅니다.
    1-7. 10의 for을 돌고 나면 repeat으로 나누어 평균을 구하고 *1000을 해서 밀리초 변경합니다.
    1-8. 그리고 그 값을 반환합니다.


2. classify
    2-1. mac 점수를 인자로 받아서 Cross | X | UNDECIDED 중 하나를 결정하는 함수입니다.
        => 두 점수는 입력 데이터가 얼마나 필터와 가까운가의 점수 -> 높으면 가까움
    2-2. 프로그래밍에서 부동소수점 비교
        => 부동 소수점은 십진수 값을 메모리에 정확하게 저장 못하는 경우가 많기 때문에 ==로 비교하면 위험합니다.
        => 컴퓨터가 정수 5는 0101로 표현가능
        => 0.1를 2진수로 바꾸면 0.000110011001100110011... 처럼 무한히 반복됩니다.
        => 즉, 0.1를 컴퓨터가 저장할 떄 무한을 저장할 수 없으니까 어느 지점에서 잘라야 합니다.
        => 따라서 실제 메모리에는 0.1과 아주 가까운 값이 저장됩니다.
        => 만약 두 부동소수점을 ==으로 비교하면 메모리에 저장된 값은 비슷한 값이라 false가 나올 수 있습니다.
        => 그래서 EPSILON을 사용합니다. -> 허용 오차
        => if abs(a - b) < EPSILON:
            print("같다고 판단")
        => 두 숫자의 차이가 0.000000001보다 작으면 사실상 같은 값으로 취급하자는 의미입니다.
        => abs 사용 -> 진정한 두 숫자의 거리 -> 비교하는 값을 그냥 양수로 두려고
    2-3. 이제 두 값의 차이가 EPSILON보다 작으면 -> 판정 불가 판단 -> "UNDECIDED"
    2-4. cross_score가 크면 "Cross", 그렇지 않으면 "X" 반환합니다.
    2-5. run_user_mode에서 classify -> classify_ab를 사용했습니다.


3. classify_ab
    => 사용자 모드일 떄 판정 함수입니다.
    => 반환값을 A나 B로 전달합니다.


3. run_user_mode
    3-1. input_matrix_3x3를 통해 첫 번쨰 필터를 입력받습니다.
    3-2. input_matrix_3x3를 통해 두 번쨰 필터를 입력받습니다.
    3-3. input_matrix_3x3를 통해 패턴을 입력받습니다.
        => 3-1 ~ 3-3은 2차원 배열입니다.
    3-4. mac 함수를 사용해서 filter_a, filter_b의 score를 받아냅니다.
        => 커스텀 입력이 커스텀 필터와 얼마나 일치하는지 점수로 반환값을
    3-5. benchmark_mac를 통해 10번 돌려서 성능을 측정합니다.
    3-6. classify_ab를 통해 어느 커스텀 필터에 가까운지 판정합니다.
    3-7. 이제 점수, 연산 시간, 판정을 출력합니다.