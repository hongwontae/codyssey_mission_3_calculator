
1. validate_matrix -> 내가 검증하고 싶은 2차원 배열 데이터
    1-1. 2차원 배열이 내가 기대하는 N*N 크기의 올바른 패턴인지 검사하는 함수입니다.
    1-2. JSON에서 가져온 input_data가 제대로 생겼는지 확인하는 역할입니다.
    1-3. input_data -> pattern["input"] -> input의 2차원 배열
    1-4. size -> pattern_name -> parts = pattern_name.split("_") -> size = int(parts[1])
        => patterns의 input의 2차원 배열이 몇 개의 행과 열로 되어 있는지 알려주는 숫자
    1-5. if not isinstance(matrix, list): -> 전체가 리스트인가?
    1-6. len(matrix) == 5 -> 행이 5개인가?
    1-7. isinstance(row, list) -> 각 행이 리스트인가?
    1-8. len(row) == 5 -> 각 행에 값이 5개인가?
    1-9. value in (0, 1) -> 각 값이 0또는 1인가?
        => 0.0 == 0 -> True, 1.0 == 1 -> Tre

    1-10. isinstance(value, 자료형)
        => value가 자료형인지 확인하는 함수입니다. 반환값은 Bool입니다.
        => isinstance(x, int) -> True
    1-11. 모든 조건을 통과하면 True를 반환합니다.


2. validate_filter_matrix -> 필터
    2-1. 필터 배열이 올바른 형태인지 검사하는 함수입니다.
    2-2. cross_filter -> cross 형태의 2차원 배열
    2-3. x_filters -> x 형태의 2차원 배열
    2-4. size는 패턴의 크기면서 필터의 크기입니다.
        => 과제에서 patterns의 키에서 n을 추출해서 해당 size N 필터를 선택하도록 요구
    2-5. 여기서도 비슷한 검사가 시작됩니다.
    2-6. 2차원 배열이 list인지, 전체 행이 size에 맞는지, 각 행이 리스트인지,
    2-7. 각 행의 요소가 size에 맞는지, 각 행의 요소가 int 또는 float인지 확인합니다.


3. run_json_mode-1
    3-1. with open을 통해 data.json을 r 모드한 파일 객체를 가져옵니다.
        => try로 묶어서 파일을 찾을 수 없거나 JSON 형식이 이상하면 return 합니다.
    
    3-2. if not isInstance(data, dict)
        => 가져오는 객체의 최상위 구조를 dict인지 확인하고 아니라면 return
    3-3. data에 patterns key, filters라는 키가 없다면 return

    3-4. 검사를 한 뒤 patterns = data["patterns"], filters = data["filters"]가져옵니다.
        => 이 떄 filters는 {
            # size는 행과 열의 개수를 의미합니다.
            size_5 : {
                cross : [2차원 배열],
                x : [2차원 배열]
            },
            size_13 : {
                cross : [2차원 배열],
                x : [2차원 배열]
            },
            size_25 : {
                cross : [2차원 배열],
                x : [2차원 배열]
            }
        }
        => 이 떄 patterns는 {
            # size_n은 행과 열의 크기이며 expected는 모양을 의미합니다.
            size_5_1 : {
                input : [2차원 배열],
                expected : "x"
            },
            size_5_2 : {
                input : [2차원 배열],
                expected : "+"
            },
            size_13_1 : {
                input : [2차원 배열],
                expected : "x"
            },
            ...
        }

    3-5. if not isinstance를 통해 가져온 patterns나 filter가 dict 형식이 아니라면 return

    3-6. total, passed, failed, fail_cases를 초기화합니다.
    
    3-7. for pattern_name, pattern in patterns.items() :
        => for문이 data.json에 들어있는 모든 패턴 케이스를 하나씩 꺼내서 검사하고 실행하는 부분입니다.
        => patterns의 key는 pattern_name으로 value는 pattern으로 들어갑니다.
        => ex) size_5_1 -> pattern_name / {input : [], expected :..} -> pattern으로

    3-8. if not isinstance (pattern, dict)
        => 만약 패턴이 객체 형식이 아니라면 Fail 처리합니다.
        => Fail 처리 
        => failed+=1/total+=1/fail.cases.append((pattern_name), reason) contine 처리입니다.

    3-9. if "input" not in pattern
    3-10. if "expected" not in pattern
        => pattern이라는 객체에 input이나 expected라는 키가 없다면 실패처리합니다.
    
    3-11. input_data = pattern["input"]
        => input_data = [2차원 배열]
    3-10. expected = pattern["expected"]
        => expected = "x" | "+"
    3-12. if expected not isinstance(expected, str):
        => expectd가 문자열이 아니라면 실패 처리합니다.
    
    3-13. parts = pattern_name.split("_")
        => ex) parts = ["size", "5", "1"]
    3-14. 만약 parts가 3개가 아니거나 0인덱스 요소가 size가 아니라면 오류 처리

    3-15. 만약 리스트가 3개가 아니거나 첫 번쨰 요소가 size라는 문자열이 아니라면
        => 실패 처리합니다.

    3-16. try + except -> size = int(parts[1])
        => part1의 값은 2차원 배열의 행과 열의 개수를 의미
        => 만약 int로 변환 -> 실패 -> ValueError 발생 -> 오류 처리

    3-17. if not vaildate_matrix(input_data, size) :
        => 만약 input_data + size 검증이 실패하면 -> 실패처리

    3-18. size_key = "_".join(parts[:2])
        => parts[:2] -> ["size", "5", "1"] -> ["size", "5"]
        => "_"join(list) -> "size_5"로 결합

    3-19. if size_key not in filters:
        => filter의 key 이름은 size_n으로 되어 있음 -> 이게 filters key에 없다면
        => 실패처리
    
    3-20. size_filter = filters[size_key] -> filters객체에 [size_key]로 접근
        => {
            cross : [2차원 배열],
            x : [2차원 배열]} -> 이런 형태를 갖춤

    3-21. if not instance(size_filter, dict)
        => 필터가 객체 형식아니라면 fail 처리
    
    3-22. normalize_filters = {}
        => 정규화된 필터를 저장하기 위한 빈 딕셔너리를 만드는 것입니다.
    
    3-23. try, for raw_label, matrix in size_filter.items()
        => pattern_name과 일치하는 size_filter를 key+value로 for
    
    3-24. label = normalize_filter_label(raw_label)
        => normalize_filter_label 함수는 raw_label 문자열의 공백을 지우고 소문자로 바꿉니다.
        => 그리고 그 값이 cross인지, x인지 판별해서 정규화하고 둘 다 아니라면 error로 처리합니다.

    3-25. normalize_filters[label] = matrix
        => 이제 정규화된 필터를 저장하기 위한 빈 딕셔너리에 정규화된 라벨을 키 : value
    
    3-26. 만약 함수 실행 중 변환 실패 -> 실패 처리

    3-27. 정규화된 객체의 키의 값이 Cross나 X가 아니라면 실패 처리

    3-19. cross_filter = size_filter["Cross"]
        => filter의 cross 2차원 배열
    3-20. x_filter = size_filter["X"]
        => filter의 x 2차원 배열
    
    3-21. vaildate_filter_matrix
        => 해당 필터가 올바른지 검사합니다.
    
    3-22. try, normalized_expected = normalize_expected(expected)
        => pattern["expected"] -> expected로 변환했었음
        => 이 값을 정규화로 또 맞춰야 함
        => 그 작업을 함수를 통해서 실행하고 변환 실패 시 실패 처리
    
    3-23. cross_score = mac(input_data, cross_filter)
        => input_data와 행과 열이 일치하는 cross_filter를 사용해서 mac 판별
    
    3-24. cross, x mac 점수 + classify를 통해 값을 얻어내고 그 결과들을 print합니다.

    3-25. 만약 제대로 되었다면 result와 normalize_expected가 같아야 합니다.
        => result는 mac 점수를 토대로 어느쪽이 가까운지에 대한 값
        => expected는 pattern에서 예측하는 값
    
    3-26. 이제 for문을 벗어나서 run_performance_analysis()를 호출합니다.
        => 3,5,13,25 int 값을 토대로 for문을 돌립니다.
    
    3-27. generate_benchmark_matrix(size)
        => matrix라는 빈 배열을 지역 변수로 
        => 내부적인 처리를 통해 size에 따른 행+열 + 1.0으로 각 값을 채웁니다.
        => 그러면 그에 따른 pattern과 filter_data가 나옵니다.
        => 그리고 benchmart_mac를 호출 -> 두 값을 인자로 넣어서
        => 시간을 재는 것입니다.
        => operation_count를 통해 mac의 연산횟수를 계산합니다.
        => 2중 for문을 사용했기 때문입니다.
        => 이 결과값을 print

    3-28. run_performance_analysis 이후에 total, passed, failed를 print()

    3-29. 만약 실패 케이스가 있다면 이에 따른 결과를 보여줍니다.


    










