1. run_json_mode-3
    1-1. performance 데이터를 가지고 최종 성능 분석 표를 출력하는 과정
    1-2. for size in sorted(performance):
        => sorted(반복가능한객체) -> 만약 dict이라면 오름차순으로 정렬됩니다.
        => for size는 performance의 key가 들어가고 5부터 반복문 시작
    1-3. average_time = sum(performance[size]) / len(performance[size])
        =>
    1-4. operation_count = size * size
        =>
    1-5. print(f"{size}x{size}\t{average_time:.3f}\t\t{operation_count}")
        =>