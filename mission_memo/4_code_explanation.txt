1. run_json_mode-2
    1-1. run_json_mode-1를 통해 모든 검사완료 합니다.
    1-2. cross_score = mac(input_data, cross_filter)
        => input_data = pattern["input"] -> 1.0, 0.0으로 이루어진 pattern 2차원 배열
        => cross_filter 
    1-3. x_score = mac(input_data, x_filter)

    1-4. cross_time = benchmark_mac(input_data, cross_filter)
    1-5. x_time = benchmark_mac(input_data, x_filter)

    1-6. operation_count = size * size